
### 2026-09-08 23:0x　ケセドの波を追った——**グロッキーが 20 秒遅い**

**動画のコマを見て、時計が合っていることが確かめられた。**`fr_ches_QnKBiKMMUQE/00100.jpg`
（映像 100 秒）の上帯は `Wave 2/2` ／ 敵の残り `8` ／ `02:50.000`。
戦闘秒 = 240 − 170 = **70 秒**なので、**映像秒 ≒ 戦闘秒 + 30。**
`hp_QnKBiKMMUQE.json` の唯一まともな行 `[176.0, 20763436, 109.3]` は
**戦闘 130.7 秒で本体 20,763,436** ——動画は 152.47 秒に討伐している。
**実物は 21.8 秒で 21,000,000 を削っている。**

核の節の時刻は `[[0,0,17.9],[29.5,1,17.9],[84.8,2,42.2],[128,3,125]]`。
**本体が湧く節 3 が 128.0 秒で、動画の 130.7 秒とほぼ一致する。**道のりは合っている。

**足りないのはグロッキー。**核は `[[152.2, 20]]`——**動画が討伐した 152.47 秒と同時。**
ケセドは素の被ダメ率が `DamagedRatio 19000`（0.1 倍）で、グロッキー中だけ
`ChesedInsanePassive01` が `−9000` して 1.0 倍に戻る。**グロッキーが来ないあいだは
何を撃っても 1/10 しか入らない。**核の本体 HP は 21,000,000 → 12,869,261 で止まる。

#### 分かったこと（数で確かめた）

**ゲージが溜まり始めるのは 132.4 秒から。**`ggLog` は 81 件あって、
**いちばん早いのが 132.4 秒**（`ChesedDroid_Decagram_AR01_Passive_Effect01`、
1 件 227 ／ 10000 必要 ＝ 44 体）。**節 0・1 で倒した数十体は 1 件も数えられていない。**

**「本体がまだ湧いていないから」ではなかった。**`R.groggyHolder` が
`living()` しか見ていなかったのを、湧いていない体も持ち主に選ぶように直した
（`intoGroggy` も、湧く前に満タンになったら `ggWait` で持ち越して湧いた瞬間に入る形にした）。
**数字は 1 の位まで動かなかった。**

**死に技は撃たれている。**死んだ敵で `onDead` が空だった回数を数える印
（`R.miss['死に技なし:<dev>']`）を足したが **0 件**。つまり
**`GroggyGaugeEffectDAO` が積まれる手前で落ちている。**

    ChesedDroidDecagramARInsanePassiveSkill01
      EssentialCandidate { TargetSide: "Self",  ApplyEntityType: 1, MaxTargetCount: 1 }   ← 引き金
      （もう 1 つ）      { TargetSide: "Ally",  ApplyEntityType: 5 }                      ← 配る先

**次に見るのはここ。**`ApplyEntityType 5`（ビットで 1|4）の読み方と、
節 0・1 の死でこの効果が読まれない理由。`readAll` / `atLevel` / 条件式のどれかで
落ちている。**`ggLog` に 1 件も出ないので、効果そのものが読まれていない。**
